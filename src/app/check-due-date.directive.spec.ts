import { CheckDueDateDirective } from './check-due-date.directive';

describe('CheckDueDateDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckDueDateDirective();

    expect(directive).toBeTruthy();
  });
});
